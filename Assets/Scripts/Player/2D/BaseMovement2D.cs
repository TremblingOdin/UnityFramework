using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BaseMovement2D : Player
{
    // Update is called once per frame
    void Update()
    {
        if(GameController.Instance.GetType<KeyboardToEventHelper>(GameTypeTitle.KEYBOARD))
    }
}
